defmodule Keyverse do
  @moduledoc """
  keyverse multipack door — Elixir/OTP host.

  Pack directories under `PACK_DIR/{multiword-key}/` are the source of truth.
  The browser capture UX is served from `priv/static` (extracted from the
  reference door) with the same `window.BASE` scoping.
  """
end
