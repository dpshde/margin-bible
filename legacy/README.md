# legacy/

Previous Keyverse: Elixir/OTP multipack door, Expo React Native client, pack-on-disk protocol (`PROTOCOL.md`).

The product is now **Margin** (margin.bible), the Rails app at the repository root. This tree is kept for protocol history, BSB/KJV sources (copied to `vendor/scripture`), and the mobile UX that the Rails reader follows (chapter + pericope headings + inline notes).

Do not deploy this directory.
