#!/usr/bin/env bash
# Start Margin on Vercel Fluid / any container that sets PORT (default 80).
set -euo pipefail

cd /rails
export PORT="${PORT:-80}"

# Image already has a seeded sqlite. These are cheap no-ops when current.
bin/rails db:prepare
bin/rails margin:seed_scripture

exec bundle exec puma -C config/puma.rb
