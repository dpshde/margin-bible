#!/usr/bin/env bash
# Start Margin in a container that sets PORT (Railway default 80).
set -euo pipefail

cd /rails
export PORT="${PORT:-80}"

# Image already has a seeded sqlite. One process: no-ops when current.
bin/rails db:prepare margin:seed_scripture

exec bundle exec puma -C config/puma.rb
